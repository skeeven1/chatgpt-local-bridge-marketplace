# ChatGPT Local Bridge Plugin 16.3.0

Desktop-only local plugin for ChatGPT. It bundles the bridge MCP server and a skill that tells ChatGPT to choose local tools automatically from normal user language.

The plugin intentionally exposes structured Windows/workspace actions only; it does not expose an arbitrary remote shell.
